// Compatibility for the Campfire module already in this repository.
// All imports resolve to the same configured client.
export { supabase, default, isSupabaseConfigured } from './lib/supabaseClient.js';
